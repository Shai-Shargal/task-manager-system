# Backend API URL (must match backend PORT in backend/.env)
VITE_API_BASE_URL=http://localhost:5001

# Set to true for frontend-only demo (in-memory mock API). Production build uses .env.production.
# VITE_DEMO_MODE=false
