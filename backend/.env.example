# HTTP server (use 5001 on macOS — port 5000 is often used by AirPlay)
PORT=5001

# MSSQL connection (Docker Azure SQL Edge default)
DB_USER=sa
DB_PASSWORD=YourStrongPassword123!
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=TaskManagerDB
